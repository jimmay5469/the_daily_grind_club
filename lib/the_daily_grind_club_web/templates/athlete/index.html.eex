<% total_days = utc_day_of_year %>
<%= for athlete <- @athletes do %>
  <strong><%= athlete.first_name %> <%= athlete.last_name %></strong>: <%= active_days(athlete) %>/<%= total_days %> days this year (<%= activity_count(athlete) %> activities)
<% end %>
